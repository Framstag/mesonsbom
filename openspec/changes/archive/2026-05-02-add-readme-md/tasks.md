## 1. Documentation Creation

- [x] 1.1 Create the top‑level `README.md` file in the repository root.
- [x] 1.2 Add a **Purpose** section that briefly describes the mesonsbom application.
- [x] 1.3 Add a **Required Build Dependencies** section listing `nlohmann_json`, `cxxopts`, `catch2` (and any future dependencies).

## 2. Build Instructions

- [x] 2.1 Write step‑by‑step Meson build commands (e.g., `meson setup build && ninja -C build`).
- [x] 2.2 Include optional build‑system flags and notes on cleaning the build directory.

## 3. Usage Guide

- [x] 3.1 Provide an example invocation of `mesonsbom --build-dir <path>`.
- [x] 3.2 Show expected output (`sbom.json`) and a brief verification command.

## 4. FAQ Section

- [x] 4.1 Add common troubleshooting items (e.g., missing `meson-info` directory, dependency version mismatches).
- [x] 4.2 Include guidance on updating the README when new dependencies are added.

## 5. Review & Finalisation

